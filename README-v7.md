# ЗОНА-40 — совместимость с LORE MANAGER v7

Основной сайт обновлён для совместимости с новым генератором.

`index.html`, `style.css` и `script.js` остаются основной оболочкой сайта; генератор публикует содержимое в `lore/`.
